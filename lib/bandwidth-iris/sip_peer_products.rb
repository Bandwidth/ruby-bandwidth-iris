module BandwidthIris
  SIPPEER_PRODUCTS_PATH = 'products'

  class SipPeerProducts
    extend ClientWrapper
    include ApiItem

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications

    def self.get_applications(client, site_id, sippeer_id)
      data = client.make_request(:get, client.concat_account_path(APPLICATIONS_PATH))
      return data
    end
    wrap_client_arg :get_applications
